import React from "react";
import { icons } from "utils";

const CardIcons = () => {
    return (
        <div className="icons">
            <span>{icons.x}</span>
            <span>{icons.checkMark}</span>
        </div>
    );
};

export default CardIcons;
