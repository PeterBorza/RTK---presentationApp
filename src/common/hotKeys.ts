import { NavLinkUrls } from "app";

export const features: Record<string, NavLinkUrls> = {
    H: NavLinkUrls.HOME,
    F: NavLinkUrls.FEATURES,
    P: NavLinkUrls.PHOTOS,
    T: NavLinkUrls.TESTER,
    U: NavLinkUrls.UTILITIES,
};
