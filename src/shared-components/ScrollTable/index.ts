import ScrollTable from "./ScrollTable";
import Header from "./Header";
import Body from "./Body";
export { default as Table } from "./Table";

export default Object.assign(ScrollTable, { Header, Body });
