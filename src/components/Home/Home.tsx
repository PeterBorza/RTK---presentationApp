import React from "react";
import SlideText from "../../reusables/SlideText";
import styles from "./Home.module.scss";

const Home = () => {
	return (
		<div className={styles.container}>
			<SlideText />
		</div>
	);
};

export default Home;
